<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.attendanceapp"
    tools:ignore="ExtraText">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendanceApp">
        <activity android:name=".Authentication.StudentAttendance"></activity>
        <activity android:name=".Authentication.Data.TakeAttendance" />
        <activity android:name=".Authentication.Data.AttendanceData"></activity>
        <activity android:name=".Authentication.models.HomePage" />
        <activity android:name=".Authentication.models.ClassInformation" />
        <activity android:name=".Authentication.DashBoardActivity" />
        <activity android:name=".Authentication.SignIn" />
        <activity android:name=".Authentication.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>